# Palo Alto "key=value" pairs
[pan-kv]
DELIMS = " "
FIELDS = message

# Example CSv to map Windows domain\user to user field (optional placeholder)
[extract-user-sam]
REGEX = (?i)(?:Account\s+Name|SubjectUserName|TargetUserName)[^:]*:\s*(?:(?:[^\\]+)\\)?([^\s]+)
FORMAT = user::$1
